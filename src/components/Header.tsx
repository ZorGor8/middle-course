// src/components/Header.tsx
import React from 'react'; // <-- Импортируем React, чтобы использовать FC

const Header: React.FC = () => {
  return (
    <header>
      <h1>Мой первый проект на React + TypeScript!</h1>
    </header>
  );
};

export default Header;